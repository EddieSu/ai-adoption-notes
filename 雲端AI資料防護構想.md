# 雲端 AI 資料防護構想

## 一、問題：成本 vs. 能力 vs. 外洩風險的三難

| 選項 | 能力 | 成本 | 資料風險 |
|------|------|------|----------|
| 雲端 AI | ⭐⭐⭐ 強 | 💲 低 | ⚠️ 怕外洩 |
| 自建 AI | ⭐⭐ 中 | 💲💲💲 高 | ✅ 可控 |
| 低階/便宜 AI | ⭐ 弱 | 💲 低 | ✅ 可控 |

> **核心構想**：選用「雲端 AI」，但在資料離開公司前加一層**閘道**——
> 透過**工具界接**控制上傳/對話內容、靠**信譽廠商承諾**做合約保障、
> 並用**資料分級**確保「最機密資料」根本不進入對話，把外洩風險壓到可接受範圍。

---

## 二、資料流與防護閘道（主架構）

```mermaid
flowchart TD
    User["員工 / 業務流程"] --> Classify{"資料分級閘道<br/>判斷敏感等級"}

    Classify -->|"最機密<br/>客戶個資 / 原始碼 / 財報"| Block["🚫 禁止進入對話<br/>僅留在內部系統"]
    Classify -->|"一般 / 可去識別化"| Gateway["工具界接層<br/>DLP Gateway"]

    Block -.->|"只送：指標 / 摘要 / 遮罩後欄位"| Gateway

    Gateway --> Mask["去識別化 / 遮罩 / Tokenize"]
    Gateway --> Tool["工具呼叫 / RAG<br/>只取回答所需片段"]
    Gateway --> Audit["留存稽核紀錄 Log"]

    Mask --> Cloud
    Tool --> Cloud

    subgraph Cloud["雲端 AI ＋ 信譽廠商承諾"]
        Vendor["企業級合約<br/>不用於模型訓練<br/>資料駐留 / 加密 / 期限刪除"]
        LLM["大型語言模型"]
        Vendor --- LLM
    end

    Cloud --> Resp["AI 回應"] --> User
```

---

## 三、三層縱深防禦（為什麼風險可被壓低）

```mermaid
flowchart LR
    subgraph L1["第 1 層：資料分級"]
        A["最機密資料<br/>不出公司網域"]
    end
    subgraph L2["第 2 層：工具界接"]
        B["遮罩 / 去識別化<br/>RAG 只送必要片段<br/>DLP 攔截 + 稽核"]
    end
    subgraph L3["第 3 層：廠商承諾"]
        C["企業合約：不訓練<br/>資料駐留 / 加密 / 刪除<br/>SOC2 / ISO 認證"]
    end

    L1 --> L2 --> L3 --> Risk["剩餘風險<br/>降到可接受"]
```

| 防線 | 手段 | 擋住什麼 |
|------|------|----------|
| 資料分級 | 事前界定「最機密」清單，源頭阻擋 | 最嚴重的整批外洩 |
| 工具界接 | 遮罩、Tokenize、RAG 片段化、DLP、稽核 | 不慎貼上的敏感欄位 |
| 廠商承諾 | 企業合約、不訓練、資料駐留、第三方認證 | 廠商端二次利用 / 留存 |

---

## 四、誠實的提醒（剩餘風險）

此構想能**大幅降低**風險，但無法做到 0：

- **分級準確度是命門**：若「最機密」清單界定不全，閘道就攔不到——需定期檢視。
- **工具界接會有漏網**：自由文字對話仍可能夾帶敏感資訊，DLP 規則需持續調校。
- **承諾 ≠ 技術隔離**：合約是「事後究責」，不等於「事前無法存取」；高度敏感場景仍建議評估
  廠商的私有部署 / VPC 方案。
- **建議搭配**：權限最小化、對話保留期限、員工教育訓練、抽樣稽核。

> 結論：以「**雲端能力 + 三層防護**」取代「全自建」，是**成本與安全的務實折衷**——
> 適合多數一般與內部資料；真正不能外流的那一小撮「最機密」，靠分級從源頭擋下。
